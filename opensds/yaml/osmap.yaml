###  opensds/osmap.yaml
# -*- coding: utf-8 -*-
# vim: ft=jinja

Ubuntu:
  backend:
    block:
      container:
        cinder:
          makefile:
            platform: 'ubuntu:xenial'

CentOS:
  pkgs:
    - https://centos7.iuscommunity.org/ius-release.rpm
