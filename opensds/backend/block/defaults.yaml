### opensds/backend/block/defaults.yaml
# vim: sts=2 ts=2 sw=2 et ai

opensds:
  backend:
    block:
      ids:
        - cinder
    container:
      cinder:
        image: openstack/cinder-blockbox
        version: stable/rocky
        compose:
          platform: 'debian-cinder:latest'
          tag: 'debian-cinder:latest'
          image: 'debian-cinder'
          imagetag: 'lvm-debian-cinder'
          dbports: '3307:3306'
          volumegroup: 'cinder-volumes'
    daemon:
      cinder:
        strategy: repo-container-compose-config-build-systemd
        repo:
          url: https://github.com/openstack/cinder.git
          branch: stable/rocky
          build_subdir: contrib/block-box
          build_cmd: make blockbox
          binaries_subdir: ''
          clean_cmd: make clean
        start: /usr/local/bin/docker-compose up -d && sleep 22222222222222222222
        stop: /usr/local/bin/docker-compose down -d
    pips:
      - notario
