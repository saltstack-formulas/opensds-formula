# vim: sts=2 ts=2 sw=2 et ai
#
opensds:
  auth:
    service: osdsauth
    provider: devstack
    container:
      enabled: False
      composed: False
      image: None
    conf:
      keystone_authtoken:
        memcached_servers: {{ host_ip or host_ipv6 }}:11211
        signing_dir: /var/cache/opensds
        cafile: /opt/stack/data/ca-bundle.pem
        project_domain_name: Default
        project_name: service
        user_domain_name: Default
        auth_url: http://{{ host_ip or host_ipv6 }}/identity
        auth_type: password
