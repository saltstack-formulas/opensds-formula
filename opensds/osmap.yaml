###  opensds/osfamilymap.yaml
# -*- coding: utf-8 -*-
# vim: ft=jinja

Default:
  dock:
    block:
      cinder:
        container:
          build: docker build https://git.openstack.org/openstack/loci.git --build-arg PROJECT=cinder --build-arg PROFILES="lvm ceph" --tag debian-cinder

Centos:
  pips:
    - python-cinderclient 
    - python-brick-cinderclient-ext
  pkgs:
    - docker-compose
    - device-mapper-persistent-data
  dock:
    block:
      cinder:
        container:
          build: docker build https://git.openstack.org/openstack/loci.git --build-arg FROM=centos:7 --build-arg PROJECT=cinder --build-arg PROFILES="lvm ceph" --tag debian-cinder

